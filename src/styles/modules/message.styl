@import "../common/var.styl"
.bin-message {
  font-size: 14px;
  position: fixed;
  z-index: 1010;
  width: 100%;
  top: 30px;
  left: 0;
  pointer-events: none;
}
.bin-message-notice {
  box-sizing: border-box;
  padding: 8px;
  text-align: center;
  transition: height .3s ease-in-out, padding .3s ease-in-out;
  .bg-wrap {
    background: #fff;
    display: inline-block;
    border-radius: 5px;
  }
}

.bin-message-notice-content {
  display: inline-block;
  pointer-events: all;
  padding: 16px;
  border-radius: 5px;
  box-shadow: 0 1px 6px rgba(0, 0, 0, .15);
  background: #fff;
  position: relative;
  i {
    vertical-align: baseline;
    margin-right: 10px;
  }
}

.bin-message-notice-close {
  position: absolute;
  right: 0;
  top: 16px;
  color: #999;
  outline: 0;
  cursor: pointer;
  i {
    font-size: 22px;
    color: #999;
    transition: color .2s ease;
    position: relative;
    top: -3px;
  }
}


.bin-message-notice-closable {
  .bin-message-notice-content {
    position: relative;
    padding-right: 32px;
  }
}
.bin-message-notice-primary {
  border: 1px solid  $color-primary-light5;
  background-color: $color-primary-light7;
  i {
    color: $color-primary;
  }
}
.bin-message-notice-info {
  border: 1px solid  $color-primary-light4;
  background-color: #ffffff;
  i {
    color: $color-primary;
  }
}
.bin-message-notice-success {
  border: 1px solid  $color-success-light5;
  background-color: $color-success-light7;
  i {
    color: $color-success;
  }
}
.bin-message-notice-warning {
  border: 1px solid  $color-warning-light5;
  background-color: $color-warning-light7;
  i {
    color: $color-warning;
  }
}
.bin-message-notice-danger {
  border: 1px solid  $color-danger-light5;
  background-color: $color-danger-light7;
  i {
    color: $color-danger;
  }
}
// move-up
.move-up-enter-active,
.move-up-leave-active {
  transition: all .3s cubic-bezier(.55, 0, .1, 1);
}
.move-up-enter,
.move-up-leave-to {
  transform-origin: 0 0;
  transform: translateY(-100%);
  opacity: 0;
}
