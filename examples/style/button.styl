section {
  padding: 20px;
}
.demo-block {
  .demo-button {
    margin-bottom: 10px;
    &:last-child {
      margin-bottom: 0;
    }
    .bin-button-group .bin-button + .bin-button {
      margin-left: 0;
    }
    .bin-button-group + .bin-button-group {
      margin-left: 10px;
    }
  }
  .bin-button + .bin-button {
    margin-left: 10px;
  }
}
